配置MDX代码块语法高亮，要求：

**功能：**
1.自动检测语言（JavaScriipt、Python、Go、Rust等）
2.行号显示（可选）
3.复制代码按钮
4.主题：
- 浅色模式：github主题
- 深色模式：dracula主题

**使用shiki库实现：** 
-支持100+语言
—速度更快
-更好的主题支持

**MDX代码块示例：**
```javascript
// 这里的代码会被高亮显示
export default function BlogPost({params}: {params: {slug: string}}) {
    const post = getPostBySlug(params.slug)
  return <article>{post.content}</article>;
}
```

请生成shiki配置和MDX组件代码
