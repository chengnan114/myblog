---
title: "Vue3 项目 CI/CD 部署实战（无 CDN，纯 Docker）"
date: "2025-03-24"
description: "从零搭建 Vue3 + Vite 项目的 GitLab CI/CD 自动化部署流程，使用 Docker 多阶段构建 + Nginx 托管，适合内部系统和小型项目。"
tags: ["Vue3", "Docker", "CI/CD", "GitLab", "Nginx", "部署"]
---

适用于内部系统、后台管理等不需要 CDN 加速的场景。整套方案只需要 4 个配置文件，push 代码后自动构建部署。

## 架构总览

```
开发者 git push main
      ↓
GitLab CI/CD 自动触发
      ↓
┌── build stage ────────────────────┐
│  docker build（读取 Dockerfile）   │
│  ├── Node 环境打包                 │
│  └── 产物 COPY 到 Nginx 镜像       │
│  docker push → 镜像仓库           │
└───────────────────────────────────┘
      ↓
┌── deploy stage ───────────────────┐
│  SSH 到服务器                      │
│  docker pull → docker run         │
└───────────────────────────────────┘
      ↓
用户 → 宿主机 Nginx(:80) → Docker Nginx(:8080) → 静态文件
```

## 需要创建的文件

```
my-vue-app/
├── src/
├── package.json
├── vite.config.ts
├── .dockerignore         ← 新建
├── nginx.conf            ← 新建（容器内 Nginx）
├── Dockerfile            ← 新建
└── .gitlab-ci.yml        ← 新建
```

---

## 1. `.dockerignore`

```
node_modules
dist
.git
.gitignore
*.md
.vscode
.env*.local
```

排除无关文件，减小构建上下文体积，加速构建。

---

## 2. `nginx.conf`（容器内 Nginx 配置）

⚠️ 这不是宿主机的 Nginx，是 Docker 容器内部的。

```nginx
server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # SPA 路由支持
    # Vue Router history 模式刷新会 404，让所有路径回退到 index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源长期缓存
    # Vite 打包带 hash（如 app-a1b2c3.js），内容变了文件名就变
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        gzip on;
        gzip_types text/css application/javascript application/json image/svg+xml;
    }

    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;

    # 禁止访问隐藏文件
    location ~ /\. {
        deny all;
    }
}
```

---

## 3. `Dockerfile`（多阶段构建）

```dockerfile
# ========== 阶段 1：构建 ==========
FROM node:20-alpine AS builder
WORKDIR /app

# 先复制 package 文件 → 利用缓存层
COPY package.json package-lock.json ./
RUN npm ci

# 复制源码并打包
COPY . .
RUN npm run build

# ========== 阶段 2：运行 ==========
FROM nginx:1.25-alpine AS runner

RUN rm -rf /usr/share/nginx/html/*
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

### 为什么用两阶段？

| | 单阶段 | 两阶段 |
|--|-------|-------|
| 最终镜像 | Node + npm + node_modules + 源码 + dist | 只有 Nginx + dist |
| 大小 | ~1 GB | ~30 MB |
| 安全 | 暴露源码 | 只有生产文件 |

---

## 4. `.gitlab-ci.yml`

```yaml
variables:
  IMAGE_NAME: $CI_REGISTRY_IMAGE
  DEPLOY_SERVER: "47.xxx.xxx.xxx"   # 替换为你的服务器 IP

stages:
  - build
  - deploy

# ===== 构建 Docker 镜像 =====
build:
  stage: build
  image: docker:24
  services:
    - docker:24-dind
  before_script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
  script:
    - docker build -t $IMAGE_NAME:$CI_COMMIT_SHORT_SHA -t $IMAGE_NAME:latest .
    - docker push $IMAGE_NAME:$CI_COMMIT_SHORT_SHA
    - docker push $IMAGE_NAME:latest
    - echo "✅ 镜像构建完成"
  only:
    - main

# ===== 部署到服务器 =====
deploy:
  stage: deploy
  image: alpine:latest
  before_script:
    - apk add --no-cache openssh-client
    - mkdir -p ~/.ssh
    - echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
    - chmod 600 ~/.ssh/id_rsa
    - ssh-keyscan -H $DEPLOY_SERVER >> ~/.ssh/known_hosts
  script:
    - |
      ssh root@$DEPLOY_SERVER << 'EOF'
        docker login -u gitlab-ci-token -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
        docker pull $IMAGE_NAME:latest
        docker stop vue-app || true
        docker rm vue-app || true
        docker run -d \
          --name vue-app \
          -p 8080:80 \
          --restart always \
          $IMAGE_NAME:latest
        docker image prune -f
        echo "✅ 部署完成"
      EOF
  only:
    - main
```

---

## 5. GitLab CI/CD Variables

在 GitLab → Settings → CI/CD → Variables 中添加：

| Variable | 值 | 属性 |
|----------|---|------|
| `SSH_PRIVATE_KEY` | 服务器 SSH 私钥内容 | Protected + Masked |

> `CI_REGISTRY`、`CI_REGISTRY_USER`、`CI_REGISTRY_PASSWORD` GitLab 自动注入，无需手动设置。

---

## 6. 宿主机 Nginx 反代

文件 `/etc/nginx/conf.d/vue-app.conf`：

```nginx
server {
    listen 80;
    server_name your-domain.cn;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

```bash
nginx -t && nginx -s reload
```

---

## 完整执行时序

```
git push main
      ↓
GitLab 读取 .gitlab-ci.yml
      ↓
┌── build stage ──────────────────────────────┐
│  ① Runner 启动 docker:24 容器               │
│  ② docker build .                            │
│     ↓ 读取 Dockerfile                        │
│     ├── npm ci → npm run build               │
│     └── COPY dist → nginx（~30MB 镜像）      │
│  ③ docker push → GitLab Registry             │
└─────────────────────────────────────────────┘
      ↓
┌── deploy stage ─────────────────────────────┐
│  ① SSH 到阿里云服务器                        │
│  ② docker pull → 拉取最新镜像                │
│  ③ docker stop/rm → 停旧容器                 │
│  ④ docker run → 启动新容器                   │
└─────────────────────────────────────────────┘
      ↓
✅ 用户访问看到最新版本
```

---

## 请求链路

```
用户请求 https://your-domain.cn/about
      ↓
宿主机 Nginx(:80) → proxy_pass
      ↓
Docker Nginx(:8080 → 容器内 :80)
      ↓ try_files
      ├── /about 文件不存在
      └── 返回 /index.html
      ↓
Vue Router 接管 → 渲染 /about 页面
```

---

## CI 和 Dockerfile 的关系

| | `.gitlab-ci.yml` | `Dockerfile` |
|--|-----------------|-------------|
| 角色 | 流水线编排（总调度） | 镜像构建说明书 |
| 触发 | git push 自动触发 | CI 中 `docker build` 触发 |
| 运行位置 | GitLab Runner | Docker 引擎 |
| 类比 | 工厂排程表 | 产品生产图纸 |

**一句话：CI 是流水线，Dockerfile 是流水线上某一道工序的操作手册。** CI 先执行，在 build stage 中调用 `docker build`，Docker 才去读 Dockerfile 构建镜像。

---

## 本地测试

```bash
docker build -t my-vue-app:test .
docker run -d -p 8080:80 --name vue-test my-vue-app:test
# 浏览器打开 http://localhost:8080
docker stop vue-test && docker rm vue-test
```
